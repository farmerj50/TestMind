import { test, expect } from "@playwright/test";

test("Navigate to Trusted Contacts", async ({ page }) => {
    await page.goto("https://www.bes-app.com/\",\"route\":\"/\",\"specPath\":\"home.spec.ts\"}");
});
